{
  "name": "attendx",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,app -c blue,green \"npm run dev:api\" \"npm run dev:app\"",
    "dev:api": "npm run dev --prefix api",
    "dev:app": "npm run dev --prefix app"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
